{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile prototype",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/prototype"
      },
      "problemMatcher": "$tsc"
    }
  ]
}
